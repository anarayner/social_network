import React from 'react';
import {Icon, SvgIcon} from '@mui/material';

const Logo = () => {
    return (
        <div>
            <img src='logo.svg'  style={{width: "100%"}}/>
        </div>
    );
};

export default Logo;
